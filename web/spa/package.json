{
  "name": "liveresults-spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Modern SPA frontend for LiveResults, built against the existing /api/v1 JSON API",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
